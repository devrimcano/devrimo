# Devrimo audit — 9 September 2026

Three separate GPT-5.6 Luna agents at max reasoning reviewed committed revision
`c70b7a9b6fcce391689e7e933ef872e669f5e6c3`: PostHog, security, and engineering
practices. These are code findings, not evidence of an incident. Production
feature flags, PostHog project settings, and live event payloads were not inspected.
The subsequent whole-school import changes are outside this audit baseline.
The PostHog and engineering reviewers returned final reports. The security
review was bounded by the coordinator after receiving its findings; its
delivered observations were independently qualified below, rather than waiting
for further exploration or claiming an exhaustive security assessment.

## Priority findings

### P1: Sensitive error text can reach analytics

High confidence in the code path; actual sensitive production payloads unverified.
Scholar hides normal workspace tool outputs but includes raw exception text in
`$ai_error` at `backend/app/agents/scholar/hooks.py:168`. Browser error capture
also forwards API-derived exception messages at
`frontend/components/posthog-analytics.tsx:146` and
`frontend/lib/api/fetcher.ts:17`. Other raw failure paths exist in
`backend/app/assistant/events.py:106` and
`backend/app/observability/turns.py:239`.

If an upstream error echoes student records or message content, it crosses the
telemetry boundary. Replace raw exception text with allowlisted error codes,
types, request IDs, and fingerprints. Secret-pattern scrubbing does not establish
that arbitrary academic or message content is absent.

### P1: Legacy catalog writes are global for scoped campus administrators

High confidence; active only while both academic catalog ingestion and reads are
disabled. `backend/app/api/v1/knowledge_admin.py:904` permits `planning_write`
to update legacy `CourseOffering` and `CourseRule` rows without organization
scope. Campus administrators receive this permission at
`backend/app/admin/auth.py:62`; the models at `backend/app/db/models.py:614`
have no organization column. The legacy planner reads the global rows at
`backend/app/planning/service.py:738`.

An administrator authorized for one organization can modify the catalog shared
by others on this branch. Remove the legacy write for scoped administrators or
migrate its storage and consumers to tenant scope. The new reviewed path is
organization-scoped; production flag values remain unverified.

### P1 candidate: SAIS response identity is inferred from the request

High confidence in the missing check; wrong-detail behavior was not reproduced
live. `backend/vendor_patches/course_info/sais_client.py:659` parses generic
detail tables and returns the requested `course_code` at line 738. Constraint
parsing has the same request-derived identity pattern at line 547. The client
documents stateful previous-page behavior at line 1014. Existing tests reject
a stale listing page, not a plausible stale detail page.

A previous course's detail could therefore be labeled as the requested course.
Require response-derived course, term, and section identity and reject ambiguous
responses. Add stale-detail and stale-constraint regression fixtures before
treating this as a proven reproduction.

## Security review qualifications

The scoped-admin legacy catalog write is the strongest confirmed tenant-boundary
finding. Additional static observations from the security reviewer:

- **P2, high static confidence: configurable embedding destinations lack an
  operator-approved destination policy.** `knowledge_admin.py:126` accepts any
  HTTP(S) hostname for local providers; campus administrators with
  `knowledge_write` can persist it at line 701. `knowledge/embeddings.py:142`
  posts corpus text and any configured API key to that endpoint. With indexing
  enabled, this permits server-origin requests to admin-selected destinations.
  Local/private endpoints are an intended feature, so a blanket private-IP ban
  is inappropriate. Establish who may approve destinations, allow intended local
  services explicitly, and constrain worker egress. Treat exfiltration severity
  according to whether a scoped admin is authorized to choose external providers.
- **Defense in depth:** catalog database policies are role-wide, not tenant-wide
  (`backend/alembic/versions/0033_academic_catalog.py:564`). Tenant isolation
  depends on application queries and cross-tenant FK guards. This alone is not
  proof of a reachable cross-tenant API exploit.
- **Rejected exploit claim:** unvalidated knowledge links were suggested as a
  `javascript:` stored-XSS vector. The installed React DOM implementation blocks
  that scheme in URL sinks (`react-dom-client.development.js:3166`). Keep URL
  normalization as hardening; do not report this vector as confirmed stored XSS.
- Deployment can fall back to migration credentials when separate backup
  credentials are absent (`scripts/deploy-vps.sh:27`). The observed backup
  succeeded. This is a least-privilege/configuration concern, not a failed backup.

## PostHog review

All findings below have high static confidence unless otherwise stated.

| Priority | Finding and trigger | Evidence | Suggested correction |
| --- | --- | --- | --- |
| P2 | Replay/autocapture initializes app-wide; inputs are masked, but rendered sensitive text has no demonstrated masking. Actual recording depends on project settings. | `frontend/instrumentation-client.ts:17`, `frontend/components/chat/chat-shell.tsx:333` | Mask sensitive text or disable replay on these surfaces; reconcile activation with the approved privacy policy. This is not a legal compliance determination. |
| P2 | A failed/deferred catalog import can return normally and produce a successful worker-pass event. Pass health does not mean import completion. | `backend/app/academic_catalog/worker.py:264`, `backend/app/workers/runtime.py:65` | Emit safe job-scoped completed/failed/deferred outcomes in addition to pass telemetry. |
| P2 | Catalog mutations bypass the central admin event helper. Durable drafts/releases retain some history, but centralized audit/event coverage is missing. | `backend/app/academic_catalog/admin.py:73`, `backend/app/admin/audit.py:27` | Add post-commit actor/org/action/outcome events without raw catalog documents. |
| P2 | `schedule_plan_completed` is emitted around curriculum lookup rather than final solving. | `frontend/components/schedule/schedule-planner.tsx:1191`, `:1319` | Separate lookup and solve events, including partial/blocked/error outcomes. |
| P2 | Independent web observer errors omit the deployed release SHA. | `scripts/web_error_observer.py:29` | Read release attribution from the deployed release marker. |
| P3 | Raw dynamic paths remain alongside normalized route templates. | `backend/app/observability/middleware.py:224`, `frontend/instrumentation.ts:42` | Prefer route templates and allowlisted correlation fields. |
| P3 | Researcher import terminal telemetry has only a fixed service identity. | `backend/app/researchers/service.py:228` | Carry initiating actor/org/request correlation where applicable. |

Verified controls: backend identity comes from verified JWT claims rather than
the PostHog distinct-ID header; frontend identity omits student email and resets
on sign-out; backend tests clear telemetry keys. These controls do not resolve
the raw-error paths above.

## Engineering review

| Priority | Finding and qualification | Evidence | Suggested correction |
| --- | --- | --- | --- |
| P2 | Full import checkpoints serialize the entire growing step list after each operation and return it to the admin client. Scaling risk is static; no production load benchmark established. | `backend/app/academic_catalog/worker.py:287`, `backend/app/academic_catalog/service.py:2469` | Keep a bounded cursor or separate durable operation plan and return compact progress. |
| P2 | Concurrent enqueue deduplication calls `db.rollback()`, potentially discarding unrelated pending enqueues in the caller's transaction. | `backend/app/academic_catalog/service.py:1933` | Use a savepoint or conflict-safe insert preserving the outer transaction. |
| P2 | Malformed warm-hour strings fail open. | `backend/app/campus/warmer.py:127` | Validate settings at startup and fail closed on invalid values. |
| P2 | The per-pass course cap breaks only the inner term loop. | `backend/app/academic_catalog/worker.py:209` | Apply a global cap across terms, with a separate discovery allowance. |
| P2 | Published detail loads meetings/instructors/restrictions per section. | `backend/app/academic_catalog/service.py:1376` | Reuse the existing bulk component loader. |
| P2 | Summary endpoints build/filter the full term in memory before pagination. Existing small-catalog query-count tests do not prove scalability. | `backend/app/academic_catalog/service.py:2587`, `:3784` | Push filtering/counts/pagination into SQL when addressing full-school scale. |
| P2 | Untyped admin response models allow the handwritten frontend contract to drift. | `backend/app/academic_catalog/admin.py:34`, `frontend/lib/admin/catalog-types.ts:1` | Define API response models and consume generated types or exercise equivalent response-contract tests. |
| P2 | Same-commit backend builds resolve broad dependency ranges again. | `backend/requirements.txt:1`, `backend/Dockerfile:150` | Lock dependencies and use reproducible CI/release artifacts. Setup changes were explicitly deferred by the user. |
| P2 | Legacy warmer budget read/write is non-atomic. Requires concurrent legacy workers; does not affect the new database admission gate. | `backend/app/campus/warmer.py:73` | Use atomic shared admission or retire the legacy worker. |
| P3 | Creation toast reads a missing response field. Reproduced in production Safari: “Draft created for undefined.” The draft persisted correctly. | `frontend/components/admin/panels/courses-panel.tsx:667`, `backend/app/academic_catalog/service.py:2449` | Fixed in subsequent local changes by using the submitted code; not part of audited commit. |
| P3 | Planner top-level revision metadata reads `revision_id`, while the catalog supplies `course_revision_id`. | `backend/app/planning/service.py:915`, `backend/app/academic_catalog/service.py:3274` | Use explicit release/per-course revision metadata rather than the mismatched field. |

Additional agent leads requiring qualification before fixes:

- No-section thesis/untimed offerings receive a synthetic unknown section in
  `published_plan_inputs` (`service.py:4017`); projection accepts explicit untimed
  evidence (`planning/workspace.py:1109`). Confirm an actual course-level untimed
  input through the complete save path before calling this silent data loss.
  Unknown timing must not automatically be treated as untimed.
- Daily budget rows can reset pacing at midnight (`worker.py:54`). The default
  01:00–07:00 window excludes midnight, so this only affects an expanded window
  spanning midnight.
- Successful source observations have no archival bound (`retention.py:10`).
  This is a growth/retention design issue; immutable evidence references must
  remain valid before introducing deletion.
- A revision has a primary source observation (`service.py:1580`). Review the
  existing component metadata and retained observation references before
  concluding that all component provenance has been lost.

## Validation and limits

The audited deployment passed 737 backend tests, 44 extension-schema tests,
frontend checks/build, and release health checks. Those results do not cover
the missing trigger cases above. The PostHog reviewer additionally ran frontend
lint/type checking and 10 release-tooling tests. No agent exploited production,
dumped student data, or changed production configuration.

The coordinator independently checked the highest-priority telemetry path,
legacy catalog tenant boundary, SAIS request-derived identity, and the live
Safari creation result. Other findings retain the stated static-review limits.

## Remediation follow-up

The initial import button and this report were committed and pushed as
`5d9a3c3` on `codex/unified-course-catalog` before remediation began. The user
then authorized non-privacy fixes and explicitly excluded privacy changes.
Raw errors, replay/masking/consent, and raw request-path telemetry are outside
this pass.

Implemented follow-up work:

- SAIS department/thesis listings require department/term identity. Detail,
  prerequisite, replacement, and restriction reads require
  response-derived course/term identity; restrictions require section identity.
  Human-readable terms must match the official selector label. Fixtures cover
  plausible stale detail pages, wrong terms/sections, ambiguous identity, and
  verified empty rule responses. Live METU HTML compatibility was not tested.
- Scoped administrators cannot modify the legacy global catalog or configure
  server-side embedding destinations. Super administrators retain these
  capabilities. The embedding UI distinguishes configuring a provider from
  reindexing or activating an already configured generation.
- Import deduplication uses a savepoint; admin filtering/counts/pagination move
  to SQL, and section children are loaded in batches. Source-evidence links
  select the relevant component and section rather than the latest unrelated
  response. Planner provenance includes a course-to-revision mapping.
- Catalog admin endpoints have explicit response schemas; frontend contract
  checks cover their generated API types. Import progress is compact rather
  than returning every planned source operation to a polling browser.
- Worker scheduling has independent bounded course/discovery allowances,
  rejects malformed hour windows, and carries pacing across local midnight.
  The legacy warmer uses atomic admission and active METU accounts. Its budget
  counts admitted Course Info calls; only the new direct-source worker counts
  every internal HTTP attempt, including authentication and redirects.
- Migration `0034_catalog_checkpoint_offset` separates the hot progress cursor
  from the JSONB plan and preserves the cursor on downgrade. Ordinary progress
  updates leave the large value unchanged, allowing PostgreSQL to preserve its
  out-of-line storage. [PostgreSQL TOAST documentation](https://www.postgresql.org/docs/16/storage-toast.html)
  Plan expansion and retry metadata can still rewrite the plan; normalized
  operation rows remain a possible further optimization, not an implemented
  change in this pass.
- Cleanup now also reclaims superseded, unreferenced successful observations
  older than 90 days. It preserves the latest exact source answer and all
  draft/relational references, including conservative protection for evidence
  that historical capped draft references may have dropped. This is not a cap on intentionally retained
  publication history.
- Catalog pass telemetry reports idle/deferred/failed/progress/completed
  outcomes; admin events carry operation/job correlation; researcher import
  events retain initiating request context. Schedule events distinguish
  curriculum lookup from solving. The external web observer adds the active
  frontend release identifier.

Dependency locking remains subject to a pending user clarification: automatic
approval review rejected generating the lockfile because of the earlier
instruction to defer development setup. No lockfile or CI/Docker install
configuration was changed by that rejected action.

The no-section thesis candidate is not permission to infer untimed status from
missing schedule data. Role-wide database policies and the rejected React
`javascript:`-link exploit remain qualified observations, not claimed fixes.

Final local validation of the remediation:

- Full backend suite on isolated PostgreSQL: **772 passed**. Existing test JWT
  key-length warnings remain. The first run found a missing request-field
  access in draft telemetry; patch and override-removal events now use the
  persisted draft term, and the complete suite passed after correction.
- Extension-schema hardening/ownership/restore suite: **44 passed**.
- Frontend stream/planning tests: **18 passed**; TypeScript and ESLint passed.
- Frontend production build passed using **Webpack**. This is not a validation
  of the default Turbopack build path.
- OpenAPI regeneration was deterministic; release observer/tooling tests:
  **11 passed**; deployment shell syntax and diff whitespace checks passed.

These are branch-local results, not production or live METU verification.
The remediation includes migration 0034, which must be applied before running
the updated catalog worker. No production deployment was performed in this pass.
